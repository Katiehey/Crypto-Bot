mode: sandbox

exchange:
  name: binance
  sandbox: true

broker:
  type: ccxt

risk:
  max_position_pct: 0.25
  max_daily_loss: 0.03
  max_drawdown: 0.20
  cooldown_bars: 1

alerts:
  channel: telegram
  severity_levels:
    - INFO
    - WARNING
    - CRITICAL

monitoring:
  structured_logs: true
  failure_injection: enabled
