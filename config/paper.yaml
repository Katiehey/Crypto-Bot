mode: paper

exchange:
  name: paper
  fee_pct: 0.0005

broker:
  type: paper

alerts:
  channel: telegram
  severity_levels:
    - INFO
    - WARNING
    - CRITICAL

monitoring:
  structured_logs: true
